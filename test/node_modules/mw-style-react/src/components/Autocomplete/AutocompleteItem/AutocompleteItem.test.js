import React from 'react';
import renderer from 'react-test-renderer';
import ThemeProvider from '../../ThemeProvider';
import MenuItem from './AutocompleteItem';
import theme from '../../themes/corezoid_light';

test('MenuItem with label', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'visible'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('MenuItem with leftIcon and label', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'visible'}
        leftIcon={'copy'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});


test('MenuItem with rightIcon and label', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'visible'}
        rightIcon={'check'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('MenuItem with leftIcon rightIcon and label', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'visible'}
        leftIcon={'copy'}
        rightIcon={'check'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Disabled MenuItem with leftIcon rightIcon and label', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'disabled'}
        leftIcon={'copy'}
        rightIcon={'check'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Disabled MenuItem', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'disabled'}
        leftIcon={'copy'}
        rightIcon={'check'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('hidden MenuItem', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'hidden'}
        leftIcon={'copy'}
        rightIcon={'check'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('MenuItem with id', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <MenuItem
        visibility={'hidden'}
        id={'test'}
        leftIcon={'copy'}
        rightIcon={'check'}
        label={'Label'}
      />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

