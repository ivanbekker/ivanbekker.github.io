import React from 'react';
import {storiesOf} from '@storybook/react';
import WithForm from '../docs/pages/withForm';

const stories = storiesOf('HOC', module);
stories
  .add('withForm', () => (<WithForm title={'withForm'}/>));
