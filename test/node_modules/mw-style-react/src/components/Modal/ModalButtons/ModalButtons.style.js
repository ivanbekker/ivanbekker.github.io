export default (theme) => ({
  buttonsWrapper: {
    textAlign: 'right',
    '& div': {
      marginLeft: theme.modal.spacingButton
    },
    '&.disabled': {
      opacity: theme.disabledOpacity
    },
    '&.hidden': {
      display: 'none'
    }
  }
});
