import React from 'react';
import PropTypes from 'prop-types';
import './Header3.scss';

const Header3 = ({title}) => <h3 className="header3">{title}</h3>;

Header3.propTypes = {
  title: PropTypes.string.isRequired
};

export default Header3;
