import React from 'react';
import renderer from 'react-test-renderer';
import ThemeProvider from '../ThemeProvider';
import Tab from './Tab';
import TabItem from './TabItem/TabItem';
import theme from '../themes/corezoid_light';

test('Tab simple test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Tab>
        <TabItem id={'1'}/>
      </Tab>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem type test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Tab type={'underline'}>
        <TabItem id={'1'}/>
      </Tab>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem activeTabId test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Tab activeTabId={'2'}>
        <TabItem id={'1'}/>
        <TabItem id={'2'}/>
        <TabItem id={'3'}/>
      </Tab>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem size(small) test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Tab size={'small'}>
        <TabItem id={'1'}/>
        <TabItem id={'2'}/>
        <TabItem id={'3'}/>
      </Tab>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem size(medium) test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Tab size={'medium'}>
        <TabItem id={'1'}/>
        <TabItem id={'2'}/>
        <TabItem id={'3'}/>
      </Tab>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});
