import React from 'react';
import {storiesOf} from '@storybook/react';
import {select, text} from '@storybook/addon-knobs/react';
import ContentEditable from '../src/components/ContentEditable';
import infoDecorator from '../.storybook/customDecorators/infoDecorator';
import themeDecorator from '../.storybook/customDecorators/themeDecorator';

const stories = storiesOf('ContentEditable', module);
infoDecorator(stories);
themeDecorator(stories);

stories
  .add('default',
    () => (
      <ContentEditable
        type={select('Type', ['text', 'html'], 'html')}
        visibility={select('Visibility', ['visible', 'disabled', 'hidden'], 'visible')}
        placeholder={text('placeholder', 'Placeholder text')}
        value={text('Value', '<img src="https://corezoid.com/static/CorezoidProduct-ce1da2c78726bb5ce1cf53b002dac519.png" alt="icn_image.svg" onload="alert(\'hello\')"/>\n' +
          '<test/>\n' +
          '<footer>footer content</footer><div>div contendt</div>')}
      />
    )
  );
