import React from 'react';
import renderer from 'react-test-renderer';
import ThemeProvider from '../../ThemeProvider';
import TabItem from './TabItem';
import theme from '../../themes/corezoid_light';

test('TabItem simple test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <TabItem id={'1'}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem disabled test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <TabItem id={'1'} visibility={'disabled'}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem hidden test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <TabItem id={'2'} visibility={'hidden'}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem error test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <TabItem id={'3'} error={true}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem icon test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <TabItem id={'1'} icon={'time'}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('TabItem label test', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <TabItem id={'1'} label={'testLabel'}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});
