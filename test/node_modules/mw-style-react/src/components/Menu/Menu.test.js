import React from 'react';
import renderer from 'react-test-renderer';
import ThemeProvider from '../ThemeProvider';
import Menu from './Menu';
import MenuItem from './MenuItem';
import Divider from '../Divider';
import theme from '../themes/corezoid_light';

test('Menu with MenuItems', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Menu visibility={'visible'}>
        <MenuItem
          visibility={'visible'}
          leftIcon={'copy'}
          rightIcon={'check'}
          label={'Label'}
        />
        <MenuItem
          visibility={'visible'}
          leftIcon={'copy'}
          label={'Label'}
        />
        <Divider/>
        <MenuItem
          visibility={'visible'}
          label={'Label'}
        />
      </Menu>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Disabled Menu with MenuItems', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Menu visibility={'disabled'}>
        <MenuItem
          visibility={'visible'}
          leftIcon={'copy'}
          rightIcon={'check'}
          label={'Label'}
        />
        <MenuItem
          visibility={'visible'}
          leftIcon={'copy'}
          label={'Label'}
        />
        <Divider/>
        <MenuItem
          visibility={'visible'}
          label={'Label'}
        />
      </Menu>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Hidden Menu with MenuItems', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Menu visibility={'hidden'}>
        <MenuItem
          visibility={'visible'}
          leftIcon={'copy'}
          rightIcon={'check'}
          label={'Label'}
        />
        <MenuItem
          visibility={'visible'}
          leftIcon={'copy'}
          label={'Label'}
        />
        <Divider/>
        <MenuItem
          visibility={'visible'}
          label={'Label'}
        />
      </Menu>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});
