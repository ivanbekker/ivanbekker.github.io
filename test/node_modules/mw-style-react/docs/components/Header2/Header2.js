import React from 'react';
import PropTypes from 'prop-types';
import './Header2.scss';

const Header2 = ({title}) => <h2 className="header2">{title}</h2>;

Header2.propTypes = {
  title: PropTypes.string.isRequired
};

export default Header2;
