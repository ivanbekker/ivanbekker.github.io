import React from 'react';
import renderer from 'react-test-renderer';
import ThemeProvider from '../ThemeProvider';
import Label from './Label';
import theme from '../themes/corezoid_light';

test('Label with small font', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Label fontSize="small" fontWeight="normal" value="label value"/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});


test('Label with id', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Label fontSize="small" fontWeight="normal" value="label value" id="mainLabel" />
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Label with color', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Label fontSize="small" fontWeight="normal" value="label value" color="red"/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Label is hidden', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Label fontSize="small" fontWeight="normal" value="label value" color="red" visibility="hidden"/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Label is disabled', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Label fontSize="small" fontWeight="normal" value="label value" color="red" visibility="disabled"/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

test('Label with erorr', () => {
  const component = renderer.create(
    <ThemeProvider theme={theme}>
      <Label fontSize="small" fontWeight="normal" value="label value" color="red" error={true}/>
    </ThemeProvider>
  );
  const tree = component.toJSON();
  expect(tree).toMatchSnapshot();
});

