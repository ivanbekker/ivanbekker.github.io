import React from 'react';
import {storiesOf} from '@storybook/react';
import {boolean, select, text} from '@storybook/addon-knobs/react';
import Banner from '../src/components/Banner';
import infoDecorator from '../.storybook/customDecorators/infoDecorator';
import themeDecorator from '../.storybook/customDecorators/themeDecorator';

const stories = storiesOf('Banner', module);
infoDecorator(stories);
themeDecorator(stories);

const size = ['small', 'medium'];
const visibility = ['visible', 'hidden'];

stories
  .add('default',
    () => (
      <Banner
        value={text('Value (BBCode)', 'Text1 for banner [b][iurl=/]internal link[/iurl][/b] [br]Text2 for banner. [br]VeryLongLabelalksdjlakjsglakslgkaskdgjaskjdgaksjdgljasdgjasdjgalsjdglkajsdgljasd;lgja;sldkjg;laksjdg')}
        size={select('Size', size, 'small')}
        visibility={select('Visibility', visibility, 'visible')}
        error={boolean('Error', false)}
      />
    )
  );
