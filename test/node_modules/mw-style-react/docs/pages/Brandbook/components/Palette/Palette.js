import React from 'react';
import Utils from '../../../../../src/components/utils/utils';
import {palette2} from '../../../../../src/components/themes/corezoid_light';
import './Palette.scss';

const Palette = () => (
  <div className="brandbook__palette">
    {Object.keys(palette2).map(key => (
      <div className="brandbook__palette__color" key={Utils.random()} style={{background: palette2[key]}}>
        <div className="brandbook__palette__info">
          <div className="brandbook__palette__info__name">{key}</div>
          <div className="brandbook__palette__info__color">{palette2[key]}</div>
        </div>
      </div>
    ))}
  </div>
);

export default Palette;
