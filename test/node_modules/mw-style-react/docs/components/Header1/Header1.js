import React from 'react';
import PropTypes from 'prop-types';
import './Header1.scss';

const Header1 = ({title}) => <h1 className="header1">{title}</h1>;

Header1.propTypes = {
  title: PropTypes.string.isRequired
};

export default Header1;
